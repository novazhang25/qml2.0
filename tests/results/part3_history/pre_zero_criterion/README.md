# Historical Part 3 outputs — superseded

This directory preserves the files that existed before the switch to the literal, unshifted zero-energy criterion. Its old threshold-based JSON, CSV, Markdown and figures are audit evidence only and are not inputs or caches for current Part 3. Statements and execution statuses inside these snapshots describe the older revision.

Use [the current report](../../bond_length_part3/PART3_REPORT.md), [implementation audit](../../../md/PART3_CORRECTION.md) and [current overlay](../../plots/rhf_30_overlay/README.md).

The saved 30-point scans here also serve as the before-migration reference: their numerical data match the current scan exactly; only current selection metadata and presentation changed.
